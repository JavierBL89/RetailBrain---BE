services:
  backend:
    build: ./backend
    env_file: .env
    depends_on:
      - db

  db:
    image: postgres
    env_file: .env
    ports:
      - "5432:5432"
# DATABASE_URL=postgres://myuser:mypassword@db:5432/mydatabase