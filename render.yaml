services:
  - type: web
    name: orchestrator
    runtime: docker
    plan: starter
    rootDir: backend/mcps/orchestrator_mcp
    dockerfilePath: Dockerfile
    envVars:
      - key: MCP_SERVICE_TOKEN
        sync: false  # lo defines manualmente o en el dashboard
      - key: HF_SPACE_URL
        sync: false
    # Render detectará orchestrator_mcp/Dockerfile
